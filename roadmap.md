# 🗺️ AI Podium UI/UX Refactoring Roadmap

본 문서는 **[AI Podium UI/UX 디자인 헌법 (DESIGN.md)]**과 **[AGENTS.md]**를 바탕으로, 높은 시각적 피로도를 해소하고 프로덕션 수준의 워크스페이스 완성도를 확보하기 위한 단계별 리팩토링 로드맵이다.

---

## 🎯 마일스톤 개요

```
[Phase 1: 디자인 기반 다지기 (토큰 & 유틸리티)]
↓
[Phase 2: 환경 설정 모달 정돈] (최우선 과제)
↓
[Phase 3: 네비게이션 & 컨텍스트 메뉴]
↓
[Phase 4: 좌/우측 패널 및 입력 UX]
↓
[Phase 5: 중앙 SSOT 에디터 & 마크다운 렌더링]
```

---

## 📌 Phase 1: 디자인 토큰 및 글로벌 타이포그래피 정돈

*목표: 볼드체 남용 억제, 테마 CSS 토큰 규격화 및 공통 버튼/배지 유틸리티 수립*

- [x] **글로벌 폰트 웨이트(Font Weight) 리셋**
  - 본문, 라벨, 설명글, 리스트 아이템 기본값을 `font-normal (400)` 또는 `font-medium (500)`으로 하향 조정
  - `font-bold`는 모달/패널 최상단 H1, H2 타이틀 및 핵심 키워드에만 한정 적용
- [x] **배지(Badge) 토큰 규격 통일 (하드코딩 Zinc 배제)**
  - 무채색 메타 배지 표준(`bg-[var(--bg-main)] text-[var(--text-muted)] border border-[var(--border-color)]`) 클래스 확립
  - 유채색 배지는 시스템 절대 상태(성공: 에메랄드, 위험: 로즈, 경고: 앰버) 3종으로 한정
- [x] **버튼 위계 분리 (Primary vs Secondary)**
  - Primary(최종 결정용 솔리드 액센트: `bg-[#6366f1] text-white`)와 Secondary(모달 내부 인라인 액션용: `bg-[var(--bg-main)] text-[var(--text-muted)] border border-[var(--border-color)]`) 명문화
- [x] **언어 표준화 기반 마련**
  - 불필요한 괄호 안 영문 병기 전면 금지 규칙을 전 Phase 공통 적용 기준으로 명시

---

## 📌 Phase 2: 환경 설정(Settings) 모달 리팩토링 (최우선 과제)

*목표: 가장 피로도가 높은 복잡 영역을 차분하고 직관적인 엔터프라이즈급 IDE 환경으로 전환*

- [x] **2-1. [AI 엔진 설정] 탭 컬러 노이즈 제거 & 라벨 한국어화**
  - `무료 추천`, `무료 티어 제공`, `검증` 등 쨍한 컬러 캡슐 배지들을 차분한 무채색 칩으로 전환
  - 인라인 버튼(`검증`, `API 키 발급`)을 세컨더리 아웃라인 버튼으로 다운그레이드
  - 불필요한 영문 병기 제거 및 100% 정갈한 표준 한국어로 통일
  - 드롭다운, 인풋, 프리셋 칩 간의 수직 여백(`space-y-5`) 확보
- [x] **2-2. [페르소나] & [보안 및 볼트] 탭 데드 스페이스(Dead Space) 해결**
  - `시스템 프롬프트 지침` textarea 높이를 하단 여백까지 확장(`flex-1`, `min-h-[300px]`) 및 모노스페이스 폰트 적용
  - 보안 탭 항목 간 카드 레이아웃 정리 및 '로컬 워크스페이스 데이터 초기화' 위험 영역 독립 경고 박스 분리
- [x] **2-3. [계정 연동] 및 드라이브 탐색기 오버레이 정리**
  - 구글 드라이브, GitHub, 원격 서버 카드의 높이 및 액션 버튼 스타일 일치화
  - 파일 탐색기 모달의 '미연동 경고'와 '빈 목록' 중복 표시 제거 (단일 Call-to-Action 상태로 통합)
  - 핵심 상호작용 본문 뷰포트 60% 이상 확보
- [x] **2-3.5. [긴급 다이어트] AI 엔진 탭 & 역할별 모델 팝업 군살 제거**
  - 구글 모델 3개 카드 삭제 및 깔끔한 1줄짜리 Select 드롭다운으로 교체 (스펙/가격은 옵션 텍스트로 축소)
  - Ollama CORS 가이드 터미널 코드와 OS 탭을 1줄짜리 아코디언(`CORS 설정 가이드 보기 ∨`)으로 격리
  - 역할별 모델 지정 팝업 4개 그리드 카드를 단일 4행 리스트/테이블 구조로 전환 및 알록달록한 권장 뱃지 제거
- [x] **2-4. [저장소 및 DB] 탭 게이지 및 카드 정렬**
  - 상단 2분할 카드(`로컬 DB`, `영구 저장 모드`)의 패딩과 라운딩 일치화
  - 하단 최종 버튼(`저장 및 적용`)만 단독 Primary 액센트 유지 및 중복 CTA 제거
- [x] **2-5. [AI 엔진 설정 긴급 평면화] 중첩 박스 제거 및 1줄 폼 필드 전환**
  - 외곽 테두리 카드 박스 완전 삭제 및 평면(Flat) 폼 구조로 전환
  - 모델 선택: '기본 모델' 라벨 + 단일 모델명 드롭다운 + 하단 12px 은은한 스펙 보조 텍스트 1줄
  - 노란색 동전 아이콘의 중복 '토큰 지표' 박스 및 초록 배지 전면 제거
  - 모든 라벨 및 배지에서 볼드체를 제거하고 font-medium/normal로 통일
- [x] **2-6. [역할별 AI 모델 지정 팝업 초경량 리팩토링] VS Code/JetBrains 스타일 단일 행 테이블**
  - 높이 축소(`py-3`) 및 `[좌측: 아이콘 + 역할명 + 1줄 설명]` / `[우측: 드롭다운]` 1:1 수평 배치
  - 박스 테두리 제거 및 `divide-white/[0.06]` 은은한 단일 구분선 적용
  - 2번째 줄 긴 설명 및 3번째 줄 권장 문구 전면 삭제, 1줄 핵심 용도로 압축
  - 셀렉트 박스 내부 괄호 수식어 제거 및 순수 모델명만 노출
  - 환경 설정 창에서 역할 모달 열 때 `onClose()` 호출 제거로 팝업 닫힘 시 환경 설정 창 정상 유지
- [x] **2-7. [AI 엔진 설정 탭 최종 미세 튜닝] 상단 위계 정리 & 슬라이더 안정화 & PDF 파서 평면화**
  - 상단 모드 전환기(`[클라우드 API | 로컬 Ollama]`) 좌측 상단 배치로 자연스러운 공급자 위계 연결 및 불필요한 '엔진 제공자' 헤더 제거
  - 세부 파라미터(온도, 최대 출력 토큰, Top-P, 스트리밍)를 가로 3분할에서 행(`py-2`) 단위 레이아웃으로 변경하여 찌그러짐 방지 및 우측 수치 정렬 안정화
  - 하단 'PDF 문서 파싱 엔진' 외곽 박스 테두리를 제거하고 얇은 상단 구분선 기반의 평면 폼 구조로 정돈
- [x] **2-8. [도움말 툴팁 최적화] 헤더 삭제 및 1px 다크 IDE 테두리 정제**
  - 툴팁 상단 물음표 아이콘, 타이틀, 도움말 배지, 그라디언트 라인 전면 삭제
  - 네온 보라색 2px 보더를 걷어내고 메인 테마의 얇고 은은한 1px 보더(`border-[#2e3142]`) 및 화살표 적용
  - 배경을 `--bg-surface`(`#1e202b`)로 통일하고 본문 텍스트 여백과 가독성 최적화
- [x] **2-9. [AI 엔진 설정 탭 레이아웃 압축 및 노이즈 제거] 인라인 정렬 & 자잘한 구분선 제거**
  - 우측 상단 '공급자별 API 키 직접 연동' 텍스트 및 불필요한 가로선 완전 삭제
  - 공급자 탭 하단 메인 보더만 유지하고 검색 연동, 추론 모드, PDF 파서의 자잘한 수평 구분선(Divider)을 전면 제거하여 여백 기반 정렬
  - 모델 드롭다운을 `flex-1` 너비로 우측 '[역할별 모델 지정 ↗]' 버튼과 1줄 수평 인라인 배치 및 라벨을 '모델'로 축소
  - 모델 스펙 보조 문구를 드롭다운 바로 아래(`pl-[66px]`)에 유지
  - 세부 파라미터 영역 내부 가로 구분선(`divide-y`)을 모두 제거하고 행 간격을 `py-1.5`로 정돈
- [x] **2-10. [계정 연동 탭 버튼 위계 수정] 세컨더리 아웃라인 전환 및 하단 주 액션 강조**
  - 계정 연동 탭 우측 3개 버튼([구글 드라이브 연결], [연결 설정], [서버 설정])의 고채도 솔리드 보라색 배경 제거
  - `bg-zinc-800/80 hover:bg-zinc-700 border-zinc-700/60` 세컨더리 아웃라인 스타일로 다운그레이드
  - 하단 [저장 및 적용] 버튼을 화면 내 단 하나의 Primary 액션으로 유지하며 차분한 인디고(`bg-indigo-600 hover:bg-indigo-500`)로 톤 조율
- [x] **2-11. [페르소나 탭 평면화 및 박스-인-박스 제거] 표준 폼 레이아웃 구축**
  - 상단 2개 필드(어시스턴트 이름, 역할 및 어조)의 불필요한 외곽 카드 래퍼를 전면 제거하고 2열 그리드(`grid-cols-2 gap-4`) 표준 폼 스타일로 전환
  - 시스템 프롬프트 지침 영역의 외곽 카드 테두리를 걷어내고 상단 헤더 줄([시스템 프롬프트 지침] + [↺ 기본값 복원])을 바깥으로 분리
  - 본문 텍스트 영역을 2중 박스 없이 단일 `textarea`(`border border-white/10 rounded-lg p-3 min-h-[360px] flex-1`)로 렌더링하여 여백 확보 및 시각적 노이즈 제거
- [x] **2-12. [저장소 및 DB 탭 박스-인-박스 제거 및 평면화] 단일 계층화 및 텍스트 톤다운**
  - 하단 잉여 안내 박스("하이브리드 로컬 저장 아키텍처") 전면 삭제
  - 데이터 백업 및 복원 영역 내부 2개 사각 박스를 제거하고, 단일 카드 컨테이너 내 상/하 2개 행과 은은한 분리선(`border-b border-white/[0.06]`)으로 단순화
  - [내보내기], [가져오기], [영구 저장 모드 요청], [새로고침] 버튼을 세컨더리 아웃라인 버튼(`bg-zinc-800/80 hover:bg-zinc-700 text-zinc-200 border border-zinc-700/60`)으로 통일
  - "여유 공간 확보됨", "보호 요청 필요" 등 부수적인 텍스트의 굵기를 `font-normal`로 낮추어 시각적 위계 정돈

---

## 📌 Phase 3: 상단 네비게이션 & 드롭다운 메뉴 개선

*목표: 모바일 앱 스타일의 고채도 캡슐 버튼을 걷어내고 정밀한 데스크톱 IDE 인터랙션 구축*

- [x] **상단 메뉴 트리거(`보기 ∨` 등) 활성 상태 톤다운**
  - 고채도 보라색 배경 제거 → 은은한 `bg-white/10` 또는 텍스트 컬러 강조로 전환
- [x] **드롭다운 컨텍스트 메뉴 스타일링 (플랫 1px 보더 룩)**
  - 과도한 소프트 섀도우 대신 날렵한 1px 보더(`border border-[var(--border-color)]`)로 정제
  - 호버 아이템 배경을 솔리드 컬러에서 은은한 반투명 그레이(`hover:bg-white/10` 또는 `hover:bg-[var(--bg-surface-hover)]`)로 수정
  - 선택 항목 곡률을 `rounded-md`(4~6px)로 통일하여 캡슐 버튼 느낌 제거
  - 활성 상태 표시는 솔리드 배경 대신 체크 아이콘(✓)으로 전환

---

## 📌 Phase 4: 패널 인터페이스 & 입력 UX 최적화

*목표: 넓어진 좌측 창의 공간 활용도를 높이고 시각적 노이즈 차단*

- [x] **좌측 AI 채팅 메시지 가독성 너비 제한 (Measure)**
  - 대화 텍스트 컨테이너에 `max-w-3xl`(약 768px) 너비 제한 적용 (가독 행길이 최적화)
  - 코드 블록은 너비를 유연하게 확장 가능하도록 처리
- [x] **하단 프롬프트 입력창 툴바 재배치**
  - 좌측 입력 옵션(첨부, `@`)과 우측 전송 액션 사이의 시선 분산 해소
  - 플레이스홀더 텍스트 사이즈와 내부 패딩 밸런스 조정
- [x] **우측 파일 탐색기(Project Explorer) 노이즈 제거**
  - 하위 폴더마다 반복되는 `(문서 없음)` 등 불필요한 회색 칩 삭제
  - 빈 디렉토리는 단순 빈 상태 인라인 인디케이터로 간소화

---

## 📌 Phase 5: 중앙 SSOT 에디터 & 마크다운 렌더링

*목표: 날것의 텍스트가 아닌, 눈이 편안한 종이 서류 느낌의 문서 작업 공간 완성*

- [x] **마크다운 실시간 렌더링 (WYSIWYG / Live Preview)**
  - 원시 기호(`**`, `##`, `---`) 노출 억제 및 세련된 Typography 스타일 매핑
- [x] **문서 본문 행간 및 자간 튜닝**
  - 본문 `line-height: 1.65`, 제목 여백(H1, H2 마진 상/하 분리) 체계화
- [x] **패널 리사이징 & 포커스 모드 연동**
  - 문서 집중 모드 시 좌/우측 패널 접힘 인터랙션 최적화

---

## 🛠️ 작업 진행 규칙 (Check Rule)

1. 한 번에 여러 탭이나 복수의 Phase 코드를 일괄 수정하지 않고, **체크박스 단위(항목별)**로 정밀하게 리팩터링한다.
2. 각 세부 작업 완료 후 `lint_applet` 및 `compile_applet`으로 빌드 무결성을 검증하고, 시각적 피로도(위계, 폰트 굵기, 테마 일체감)를 평가한다.
