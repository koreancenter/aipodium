# 🏛️ AI Podium UI/UX 디자인 헌법 (Design System & Guidelines)

본 문서는 AI Podium의 모든 프론트엔드 컴포넌트, 레이아웃, 모달, 스타일링을 관통하는 최상위 원칙이다. 모든 UI 코드 작성 및 리팩터링 시 아래 규칙을 절대적으로 준수한다.

---

### 제1조: 타이포그래피 계층 (Typography Hierarchy) - 볼드체 절대 남용 금지
1. **Regular 기본 원칙**: 모든 본문, 일반 텍스트, 입력창 텍스트, 설명글, 배지 내부 텍스트는 `font-normal(400)` 또는 `font-medium(500)`을 사용한다.
2. **Bold 제한적 허용**: `font-semibold(600)` 및 `font-bold(700)`는 오직 **페이지/모달의 최상위 타이틀(H1, H2)**과 사용자의 즉각적인 식별이 필요한 **핵심 키워드**에만 허용한다.
3. **가독성(Measure)**: 긴 텍스트 문단 및 AI 대화 본문은 가독성을 위해 한 줄 최대 너비를 제한한다 (`max-w-3xl` / `max-w-[720px]`).
4. **행간과 자간**: 한글 가독성을 고려해 본문에는 반드시 적절한 행간(`leading-relaxed` 또는 `leading-6`)과 미세한 자간 축소(`tracking-tight` 또는 `-0.01em`)를 적용한다.

---

### 제2조: 테마 토큰 및 컬러 팔레트 (Theme Tokens & Color Weight)
1. **메인 IDE 디자인 토큰 연동 (하드코딩된 Zinc 계열 전면 금지)**:
   - 임의의 징크 그레이(`zinc-800`, `#18181b`, `#27272a`, `#3f3f46`) 하드코딩을 금지하고 메인 IDE 테마 변수(`var(--*)`) 및 시맨틱 유틸리티를 우선 사용한다:
     - **Base (60%) 메인 배경**: `var(--bg-main)` (`#121318`) 또는 `.bg-main`
     - **Surface & Panel (30%) 패널/모달 표면**: `var(--bg-surface)` (`#1e202b`) 또는 `.bg-surface`
     - **Editor 표면**: `var(--bg-editor)` (`#16171e`) 또는 `.bg-editor`
     - **테두리 경계선 (Border)**: `var(--border-color)` (`#2e3142`) 또는 `.border-main`
     - **호버 및 행 선택 배경**: `var(--bg-surface-hover)` (`#282a38`)
     - **Accent (10% 이하)**: 단 하나의 브랜드 주력 컬러 `var(--accent-primary)` (`#6366f1` / 인디고 퍼플)만 핵심 액션에 사용한다.
2. **배지(Badge) 색상 폭주 금지**:
   - 메타데이터 및 일반 상태 배지는 튀는 유채색을 금지하고, 무채색 톤다운 칩(`bg-[var(--bg-main)] text-[var(--text-muted)] border border-[var(--border-color)]`)으로 톤을 낮춘다.
   - 유채색 배지는 시스템의 절대적인 상태 알림(성공: 에메랄드/그린, 위험: 로즈/레드, 경고: 앰버)에만 극히 제한적으로 사용한다.
3. **호버(Hover) 효과의 절제**:
   - 드롭다운 메뉴, 탭, 리스트 아이템 호버 시 쨍한 솔리드 컬러 박스를 절대 사용하지 않는다.
   - 반드시 은은한 반투명(`hover:bg-white/5` ~ `hover:bg-white/10` 또는 `hover:bg-[var(--bg-surface-hover)]`)과 밝아지는 텍스트(`hover:text-white`)로 처리한다.
4. **그림자(Shadow) 규칙**:
   - 불필요한 소프트 섀도우를 배제하고, 날렵한 1px 테두리(`border border-[var(--border-color)]`) 중심의 하이엔드 IDE 룩을 유지한다.

---

### 제3조: 버튼 계층 구조 (Button Hierarchy)
1. **Primary Action (화면/모달당 1개 원칙)**:
   - 최종 완료/저장/결정 버튼(예: `[저장 및 적용]`, `[문서 생성]`)만 브랜드 솔리드 액센트 컬러(`bg-[#6366f1] hover:bg-[#4f46e5] text-white`)를 사용한다.
2. **Secondary & Inline Action**:
   - 내부 도구 버튼(예: `[검증]`, `[키 발급]`, `[연결]`, `[모드 변경]`, `[취소]`)은 반드시 **세컨더리 아웃라인/고스트 버튼(`bg-[var(--bg-main)] hover:bg-[var(--bg-surface-hover)] text-[var(--text-muted)] hover:text-[var(--text-main)] border border-[var(--border-color)]`)**으로 다운그레이드하여 Primary 버튼과 시각적 충돌을 방지한다.
3. **중복 CTA 배제**:
   - 동일한 액션을 실행하는 버튼(예: 모달 상단 헤더 우측 버튼과 본문 최하단 버튼)을 한 화면에 중복 노출하지 않는다.

---

### 제4조: 여백, 공간 밀도 및 뷰포트 확보 (Whitespace & Viewport Density)
1. **핵심 뷰포트 60% 이상 보장**:
   - 모달이나 패널 높이 내에서 실제 사용자가 상호작용할 본문 영역(파일 목록, 에디터, 폼, 결과 뷰어)이 최소 60% 이상 확보되도록 알림 박스와 안내 배너는 1~2줄 컴팩트 형태로 디자인한다.
2. **수직 적층(Stacking) 최소화**:
   - 헤더, 서브탭, 배너, 필터바, 안내문 등이 수직으로 겹겹이 쌓여 본문 콘텐츠가 짓눌리는 현상을 방지한다.
3. **논리적 그룹 분리**:
   - 서로 다른 성격의 설정 블록 사이에는 적절한 여백(`space-y-5` 또는 `gap-5`)을 주어 정보 덩어리를 명확히 인지할 수 있게 한다.
4. **곡률(Radius)의 통일과 IDE 조형 언어**:
   - 메인 에디터의 날렵한 1px 보더 IDE 스타일에 부합하도록 통일한다:
     - 모달/다이얼로그 컨테이너: `rounded-lg` (최대 `rounded-xl`)
     - 내부 카드/패널/입력창: `rounded-md`
     - 버튼/칩/배지: `rounded-md` (태그 외 과도한 `rounded-full` 알약형 캡슐 지양)
     - 탭 스타일: 둥근 알약형 대신 플랫한 하단 보더 탭(`border-b-2 border-indigo-500`) 또는 플랫 세그먼트 스타일 적용.

---

### 제5조: 언어 및 라벨링 표준 (순수 한국어 100% 통일)
1. **괄호 안 영문 병기 전면 금지**:
   - `(Open)`, `(Save)`, `(SSOT)`, `(Esc)`, `(.md)`, `(.html)`, `(Beta)` 등의 불필요한 괄호 영단어를 모두 제거한다.
   - 탭, 버튼, 필터 칩, 툴팁, 플레이스홀더, 안내 텍스트 모두 자연스럽고 정갈한 표준 한국어로 100% 통일한다.
2. **용어 일관성**:
   - `불러오기`, `저장`, `기본 폴더 설정`, `마크다운`, `HTML 문서`, `닫기`, `취소` 등 통일된 용어를 적용한다.
3. **시스템 내부 용어 정제**:
   - `Scope: drive.file`, `HTTP 200`, `LocalIndexedDB` 등 사용자에게 불필요한 내부 기술 코드를 노출하지 않고 친절한 사용자 친화적 설명으로 치환한다.

---

### 제6조: 마크다운 및 에디터 뷰 (Markdown / SSOT Viewer)
1. 중앙 에디터 영역은 원시 마크다운 기호(`**`, `##`, `---`)가 날것으로 노출되어 눈을 어지럽히지 않도록 정교하게 파싱된 Typography 스타일을 기본 제공한다.
2. 문서 에디터는 학술 연구 문서의 정갈함과 높은 가독성을 유지하며, 인지적 잡음을 최소화한다.
