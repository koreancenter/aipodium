# 🏛️ AI Podium UI/UX 디자인 헌법 (Design System & Guidelines v2.0)

본 문서는 AI Podium의 모든 프론트엔드 컴포넌트, 레이아웃, 모달, 스타일링을 관통하는 최상위 원칙이다. 모든 UI 코드 작성 및 리팩터링 시 아래 규칙을 절대적으로 준수한다.

---

### 제1조: 타이포그래피 및 가독성 (Typography & Contrast)
1. **Regular & Medium 기본 원칙**: 
   - 모든 본문, 일반 텍스트, 입력창 텍스트, 설명글, 배지 내부 텍스트는 `font-normal(400)` 또는 `font-medium(500)`을 기본으로 한다.
2. **Bold 극도의 절제**: 
   - `font-semibold(600)`는 오직 최상위 헤딩(H1, H2)에만 제한적으로 허용하며, 폼 라벨이나 리스트 행에 `font-bold`를 남용하지 않는다.
3. **텍스트 명도(Contrast) 보장**:
   - 어두운 배경에서 글자가 묻히지 않도록 문서 본문 및 핵심 데이터는 반드시 **고명도 오프화이트(`text-zinc-200` / `#e4e4e7` 이상)**를 사용한다.
   - 보조 설명글은 `text-zinc-400`, 플레이스홀더는 `text-zinc-500`으로 3단계 계층을 엄격히 분리한다.
4. **글자 크기 및 행간/자간 규격**:
   - 중앙 문서 본문: 15px(0.9375rem), `leading-[1.75]`, `tracking-tight`(-0.01em)
   - 모달/UI 레이블 및 일반 컨트롤: 13~14px(0.8125~0.875rem), `leading-normal`
   - 메타데이터 및 칩: 11~12px(0.6875~0.75rem)
5. **가독성 너비 제한(Measure)**:
   - 중앙 문서 에디터와 좌측 AI 대화 본문은 가로로 과도하게 늘어지지 않도록 최대 너비를 강제한다 (`max-w-3xl` / 768px).

---

### 제2조: 테마 토큰 및 컬러 팔레트 (Theme Tokens & Color Weight)
1. **선명하고 정갈한 클린 다크(Clean Dark) 베이스**:
   - 탁하고 눅눅한 네이비/퍼플 틴트 하드코딩을 배제하고, 눈이 편안한 뉴트럴 딥 다크 팔레트를 적용한다:
     - **최하단 바닥 배경 (Base)**: `#09090b` (zinc-950) 또는 `var(--bg-main)`
     - **패널 및 작업 표면 (Surface)**: `#121214` (zinc-900) 또는 `var(--bg-surface)`
     - **카드, 인풋 및 모달 표면 (Elevated)**: `#18181b` (zinc-900 light) 또는 `var(--bg-editor)`
     - **주력 액센트 (Accent, 10% 이하)**: `indigo-500` / `#6366f1` (채도가 과도하게 튀지 않는 단일 포인트)
2. **다크모드 테두리(Line) 4대 원칙**:
   - **불투명 회색 실선 절대 금지**: 경계선은 반드시 반투명 화이트(`border-white/[0.08]` 또는 `border-white/[0.06]`)를 사용해 은은한 빛 반사 느낌만 준다.
   - **사방 테두리 남용 금지**: 상하좌우를 모두 감싸는 테두리 대신, 상/하 1면 분리선(`border-b` 또는 `border-t`)을 우선 활용한다.
3. **배지(Badge) 색상 폭주 금지**:
   - 메타데이터 및 일반 칩은 무조건 **차분한 무채색 다크 칩(`bg-white/5 text-zinc-400 border border-white/10`)**으로 통일한다.
   - 유채색 배지는 시스템의 절대적인 상태 알림(성공: 에메랄드/그린, 위험: 로즈/레드, 경고: 앰버)에만 극히 제한적으로 사용한다.
4. **호버(Hover) 효과의 절제**:
   - 메뉴, 탭, 리스트 아이템 호버 시 쨍한 솔리드 컬러 박스를 절대 사용하지 않는다.
   - 은은한 반투명 화이트(`hover:bg-white/5` ~ `hover:bg-white/10`)와 밝아지는 텍스트(`hover:text-white`)로만 반응을 준다.

---

### 제3조: 버튼 계층 구조 (Button Hierarchy)
1. **Primary Action (화면/모달당 1개 원칙)**:
   - 최종 완료/저장/결정 버튼(예: `[저장 및 적용]`, `[문서 생성]`)만 브랜드 솔리드 액센트 컬러(`bg-indigo-600 hover:bg-indigo-500 text-white`)를 사용한다.
2. **Secondary & Inline Action**:
   - 내부 도구 및 연동 버튼(예: `[검증]`, `[키 발급]`, `[구글 드라이브 연결]`, `[연결 테스트]`)은 **세컨더리 아웃라인/고스트 스타일(`bg-white/5 hover:bg-white/10 text-zinc-300 border border-white/10`)**을 엄격히 적용한다.
3. **중복 CTA 배제**:
   - 한 모달 창 내에서 동일한 역할을 수행하는 액션 버튼을 헤더와 푸터에 이중으로 두지 않는다.

---

### 제4조: 레이아웃 평면화 & 공간 밀도 (Flat Layout & Density)
1. **박스-인-박스(Box-in-Box) 전면 금지**:
   - 개별 인풋 필드나 1줄 리스트 항목을 테두리가 있는 사각 카드로 감싸는 2중 래핑을 금지한다.
   - 그룹화는 박스 테두리가 아닌 **여백(`gap-5`, `space-y-6`)**과 **단일 리스트 행(Row)**으로 평면화(Flat)한다.
2. **수직 적층(Stacking) 최소화**:
   - 툴바, 탭, 배너, 안내문이 겹겹이 쌓여 실제 콘텐츠 뷰포트가 60% 미만으로 짓눌리지 않도록 컴팩트하게 인라인 정렬한다.
3. **데스크톱 IDE 표준 곡률(Radius)**:
   - 과도하게 둥근 모바일 스타일 캡슐을 제거하고 샤프한 엔터프라이즈 곡률로 통일한다:
     - 메인 작업 패널 및 스플리터: `rounded-none` (완전 직각)
     - 버튼, 인풋 필드, 카드, 드롭다운: `rounded-md` (4~6px)
     - 배지, 상태 칩, 코드 인라인: `rounded-sm` (2~4px)
     - 최상위 플로팅 모달 윈도우 외곽: `rounded-xl` (12px)

---

### 제5조: 언어 및 라벨링 표준 (순수 한국어 100% 통일)
1. **괄호 안 영문 병기 전면 금지**:
   - `(Open)`, `(Save)`, `(SSOT)`, `(Esc)`, `(.md)`, `(Beta)` 등의 불필요한 괄호 영단어를 모두 제거한다.
2. **용어 일관성**:
   - `불러오기`, `저장`, `기본 폴더 설정`, `마크다운`, `HTML 문서`, `닫기`, `취소` 등 통일된 정갈한 표준 한국어 용어를 적용한다.
3. **시스템 코드 비노출**:
   - `HTTP 200`, `IndexedDB` 등 내부 구현 코드를 일반 UI에 노출하지 않고 사용자 친화적 문구로 안내한다.

---

### 제6조: SSOT 중앙 에디터 및 분할 작업 뷰 (SSOT Workspace View)
1. **종이 서류 멘탈 모델 (Document Feel)**:
   - 중앙 에디터는 원시 마크다운 기호(`**`, `##`, `---`)의 날것 노출을 억제하고, 정교하게 파싱된 라이브 프리뷰 스타일을 기본 제공한다.
   - 에디터 컨테이너에 `max-w-3xl mx-auto`와 `px-8 py-10`의 넉넉한 호흡 여백을 주어 글쓰기 몰입도를 높인다.
2. **PDF/분할 작업 뷰의 지능적 화면 확장**:
   - PDF 파싱 등 2-Pane 이상 분할 작업 시 좌측 AI 패널을 자동으로 접어(Auto-Collapse) 최소 50:50의 쾌적한 가로 뷰포트를 확보한다.
   - 도구 모음(Toolbar)은 각 패널 상단에 분리 귀속시키며 한 줄에 6개 이상의 컨트롤을 무리하게 병합하지 않는다.