# 🏛️ AI Podium UI/UX 디자인 헌법 (Design System & Guidelines v2.1)

본 문서는 AI Podium의 모든 프론트엔드 컴포넌트, 레이아웃, 모달, 팝오버, 스타일링을 관통하는 최상위 원칙이다[cite: 17]. 모든 신규 기능 개발 및 UI 리팩터링 시 아래 규칙을 절대적으로 준수하여 "하나의 원석에서 조각해 낸 듯한 매끄러운 단일 도구"의 완성도를 유지한다[cite: 17].

---

### 제1조: 타이포그래피 및 가독성 (Typography & Contrast)
1. **Regular & Medium 기본 원칙**: 
   - 모든 본문, 일반 텍스트, 입력창 텍스트, 설명글, 배지 내부 텍스트는 `font-normal(400)` 또는 `font-medium(500)`을 기본으로 한다[cite: 17].
2. **Bold 극도의 절제**: 
   - `font-semibold(600)`는 오직 최상위 헤딩(H1, H2)에만 제한적으로 허용하며, 폼 라벨이나 리스트 행에 `font-bold`를 남용하지 않는다[cite: 17].
3. **텍스트 명도(Contrast) 보장**:
   - 어두운 배경에서 글자가 묻히지 않도록 문서 본문 및 핵심 데이터는 반드시 **고명도 오프화이트(`text-zinc-200` / `#e4e4e7` 이상)**를 사용한다[cite: 17].
   - 보조 설명글은 `text-zinc-400`, 플레이스홀더는 `text-zinc-500`으로 3단계 계층을 엄격히 분리한다[cite: 17].
4. **글자 크기 및 행간/자간 규격**:
   - 중앙 문서 본문: 15px(0.9375rem), `leading-[1.75]`, `tracking-tight`(-0.01em)[cite: 17]
   - 모달/UI 레이블 및 일반 컨트롤: 13~14px(0.8125~0.875rem), `leading-normal`[cite: 17]
   - 메타데이터, 퀵픽 리스트, 칩: 11~12px(0.6875~0.75rem)[cite: 17]
5. **가독성 너비 제한(Measure)**:
   - 중앙 문서 에디터와 좌측 AI 대화 본문은 가로로 과도하게 늘어지지 않도록 최적 가독 너비를 강제한다 (`max-w-3xl` / 768px)[cite: 17].

---

### 제2조: 테마 토큰 및 서피스 시스템 (Theme Tokens & Surface Hierarchy)
1. **옵시디언 딥 다크(Obsidian Deep Dark) 3단계 서피스**:
   - 파편화된 네이비/블루그레이 하드코딩을 배제하고, 매끄러운 3단계 무채색 서피스를 적용한다[cite: 17]:
     - **Surface 0 (최하단 바닥 Base)**: `#09090b` 또는 `var(--bg-main)` (순도 높은 딥 옵시디언 블랙)[cite: 17]
     - **Surface 1 (사이드바 및 패널 Panel)**: `#121214` 또는 `var(--bg-surface)` (정제된 다크 서피스)[cite: 17]
     - **Surface 2 (에디터 캔버스 Canvas)**: `#0c0c0e` 또는 `var(--bg-editor)` (최적의 문서 집필 공간)[cite: 17]
     - **Surface Floating (팝오버 / 모달 / 플로팅 독)**: `#16181d]/95` + `backdrop-blur-md`
     - **호버 및 활성 행 (Hover / Active)**: `hover:bg-white/[0.06]` 또는 `#18181b`[cite: 17]
2. **다크모드 경계선(Line) 3대 원칙**:
   - **불투명 회색 실선 절대 금지**: 경계선은 시스템 표준 테두리 토큰(`var(--border-color)` / `#222226`) 또는 반투명 화이트(`border-white/[0.08]`)만 허용한다[cite: 17].
   - **사방 테두리 남용 금지**: 상하좌우를 모두 감싸는 테두리 대신, 상/하 또는 좌/우 1면 분리선(`border-b`, `border-r`)을 우선 활용한다[cite: 17].
   - **면 분할 우선**: 인접한 영역 간의 구획은 두꺼운 보더 대신 서피스 명도 차이(`bg-surface` vs `bg-editor`)로 자연스럽게 구분한다[cite: 17].
3. **단일 주력 액센트(Accent) 집중**:
   - 메인 액션 및 시그니처 포인트는 **단일 인디고 바이올렛(`indigo-500` / `violet-500` / `#6366f1`)**으로 전역 통일한다[cite: 17].
   - 옐로우, 에메랄드, 블루, 퍼플 등이 한 화면에 혼재하는 시각적 오염을 금지한다[cite: 17].
   - 유채색(에메랄드/로즈/앰버)은 오직 성공/에러/경고를 알리는 **4px 초소형 상태 점(Status Dot)**에만 극도로 절제하여 사용한다[cite: 17].
4. **배지(Badge) 무채색 플랫화**:
   - 메타데이터 및 일반 칩은 무조건 **차분한 무채색 다크 칩(`bg-white/5 text-zinc-400 border border-white/5`)**으로 통일한다[cite: 17]. 불필요한 색상 배경 필(Pill)을 남발하지 않는다[cite: 17].

---

### 제3조: 버튼 및 인터랙션 계층 (Button Hierarchy & Controls)
1. **Primary Action (화면/모달당 오직 1개)**:
   - 최종 완료/시작/핵심 실행 버튼만 브랜드 솔리드 액센트 컬러(`bg-indigo-600 hover:bg-indigo-500 text-white shadow-sm`)를 사용한다[cite: 17].
2. **Secondary & Ghost Controls**:
   - 툴바, 패널 토글, 보조 버튼은 배경 없는 **고스트/플랫 스타일(`text-zinc-400 hover:text-zinc-100 hover:bg-white/[0.06] rounded-md`)**을 엄격히 적용한다[cite: 17].
3. **버튼 텍스트 단순화**:
   - 툴바 내 버튼에 긴 한글 설명(`↓↑ 스크롤 연동`, `편집`, `미리보기` 등)을 나열하지 않는다[cite: 17]. 명확한 SVG 아이콘 + 툴팁 구조로 축약하여 작업 캔버스를 최대로 확보한다[cite: 17].
4. **자동 확장 텍스트 영역 (Auto-Growing Textarea)**:
   - 프롬프트 입력창 등에 인위적인 S/M/L 수동 조절 버튼을 배치하지 않는다[cite: 17]. 입력된 텍스트 양에 맞춰 1줄(44px)부터 최대 220px까지 자연스럽게 늘어나고, 초과 시 내부 스크롤로 전환되는 규격을 기본으로 채택한다[cite: 17].

---

### 제4조: 레이아웃 평면화 & 공간 밀도 (Flat Layout & Density)
1. **박스-인-박스(Box-in-Box) 전면 금지**:
   - 개별 인풋 필드나 1줄 리스트 항목을 테두리가 있는 사각 카드로 감싸는 2중 래핑을 일체 금지한다[cite: 17].
   - 그룹화는 테두리 상자가 아닌 **단일 플랫 행(Row)**과 정제된 여백(`gap-4`, `space-y-4`)으로 해결한다[cite: 17].
2. **부유형 팝오버 및 퀵픽(QuickPick) 1줄 리스트 규격**:
   - 드롭다운, 모델 선택기, 설정 팝오버 내부의 모든 항목은 카드 형태가 아닌 **단일 행 플랫 리스트(`px-2.5 py-1.5 hover:bg-white/[0.06] rounded-md text-xs text-zinc-300 flex items-center justify-between`)**로 구성한다[cite: 17].
   - 각 행 내부에 중복되는 뱃지(예: `클라우드`)나 서브 텍스트를 중첩 삽입하지 않는다[cite: 17].
3. **IDE 3단 레이아웃 황금비율 고정**:
   - 불필요한 마우스 드래그 리사이저를 지양하고 검증된 고정 너비와 부드러운 토글(Toggle)을 적용한다[cite: 17]:
     - **좌측 대화/어시스턴트 패널**: 고정 `340px` (`w-[340px] shrink-0`)[cite: 17]
     - **우측 파일/프로젝트 탐색기**: 고정 `240px` (`w-[240px] shrink-0`)[cite: 17]
     - **중앙 문서 에디터**: 잔여 공간 100% 가변 점유 (`flex-1 min-w-0`)[cite: 17]
     - 숨김 처리 시 `w-0 overflow-hidden`으로 매끄럽게 전환된다[cite: 17].
4. **모바일/태블릿 반응형 Above-the-Fold 보장**:
   - 뷰포트 높이가 낮거나 작은 모바일 화면(`< 1024px`, `< 700px h`)에서는 장황한 홍보성 인트로보다 **핵심 액션(로그인/시작하기 카드)을 최우선 노출**한다[cite: 17].
   - 부차적인 기능 설명 블록은 축약 칩으로 전환하거나 숨김 처리하여, 사용자가 스크롤을 내리지 않고도 진입 버튼을 즉시 누를 수 있어야 한다[cite: 17].
5. **데스크톱 IDE 표준 곡률(Corner Radius)**:
   - 모바일 특유의 과도하게 둥근 캡슐을 배제하고 단정한 엔터프라이즈 곡률로 통일한다[cite: 17]:
     - 메인 작업 패널, 스플리터, 에디터 경계: `rounded-none` (직각)[cite: 17]
     - 버튼, 인풋 필드, 리스트 항목, 드롭다운: `rounded-md` (6px)[cite: 17]
     - 모달 다이얼로그 외곽: `rounded-xl` (12px)[cite: 17]
     - PDF 플로팅 오버레이 독: `rounded-full` (유일한 예외 허용)[cite: 17]

---

### 제5조: 언어 및 라벨링 표준 (순수 한국어 통일)
1. **괄호 안 영문 병기 전면 금지**:
   - `(Open)`, `(Save)`, `(SSOT)`, `(Esc)`, `(Beta)` 등의 불필요한 괄호 영단어를 모두 제거한다[cite: 17].
2. **용어 일관성**:
   - `불러오기`, `저장`, `기본 폴더 설정`, `마크다운`, `닫기`, `취소` 등 통일된 정갈한 표준 한국어 용어를 적용한다[cite: 17].
3. **시스템 코드 비노출**:
   - `HTTP 200`, `IndexedDB`, `GAPI` 등 내부 기술 코드를 일반 UI에 직접 노출하지 않고 사용자 중심의 직관적 문구로 안내한다[cite: 17].

---

### 제6조: SSOT 중앙 에디터 및 분할 작업 뷰 (SSOT Workspace View)
1. **종이 서류 멘탈 모델 (Document Feel)**:
   - 중앙 에디터는 원시 마크다운 기호의 날것 노출을 억제하고 정교하게 파싱된 라이브 스타일을 기본 제공한다[cite: 17].
   - 넉넉한 상하좌우 여백(`max-w-3xl mx-auto px-8 py-10`)으로 문서 작성 몰입도를 보장한다[cite: 17].
2. **인라인 생성(Inline Creation) 원칙**:
   - 프로젝트 및 폴더 생성 시 팝업 프롬프트나 기본값('새 프로젝트 2') 강제 주입을 금지한다[cite: 17].
   - '+' 버튼 클릭 즉시 탐색기 트리에 인라인 텍스트 인풋이 포커스된 상태로 열리며, Enter로 확정하고 Escape로 즉시 취소할 수 있어야 한다[cite: 17].
3. **PDF 분할 뷰의 화면 확보**:
   - PDF 파싱 등 2-Pane 이상 분할 작업 시 페이지 컨트롤러는 하단 중앙 플로팅 독(`Floating Dock`)으로 격리하여, 상단 툴바가 문서 에디터 영역을 침범하지 않도록 한다[cite: 17].

---

### 제7조: 데이터 주권 및 외부 연동 상태 무결성 (Integrations & State Integrity)
1. **상태 허위 표시(False-Positive) 금지**:
   - Google 로그인 상태를 타 서비스(GitHub 등)의 연동 여부와 결부시켜 거짓 뱃지(`계정 연동됨`)를 표시하지 않는다[cite: 17]. 실제 유효한 자격 증명(PAT, API Key)이 검증되었을 때만 연결 뱃지를 활성화한다[cite: 17].
2. **자격 증명 암호화 보관**:
   - 사용자가 입력한 모든 민감 키(Gemini API Key, GitHub PAT 등)는 평문으로 저장되지 않으며, `securityCrypto` 서비스를 통해 로컬 브라우저 격리 암호화 상태로 보관한다[cite: 17].