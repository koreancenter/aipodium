# AI Podium 팝업창 및 UI/UX 고도화 공통 원칙

이 프로젝트의 모든 모달 및 팝업창(설정, 파일 탐색기, 통합 다이얼로그 등)은 메인 3단 IDE 화면의 디자인 시스템과 완벽히 조화되도록 다음 원칙을 반드시 준수합니다.

---

### 1. 언어 및 라벨링 표준 (순수 한국어 통일)
- **괄호 안 영문 병기 전면 금지**:
  - `(Open)`, `(Save)`, `(SSOT)`, `(Esc)`, `(.md)`, `(.html)` 등의 불필요한 괄호 영단어를 모두 제거합니다.
  - 탭, 버튼, 필터 칩, 툴팁, 플레이스홀더, 안내 텍스트 모두 자연스럽고 정갈한 표준 한국어로 100% 통일합니다.
- **용어 일관성**:
  - `불러오기`, `저장`, `기본 폴더 설정`, `마크다운`, `HTML 문서`, `닫기`, `취소` 등 일관된 용어를 적용합니다.

---

### 2. 테마 컬러 및 디자인 토큰 일치화
임의의 징크 그레이(`zinc-800`, `#18181b`, `#27272a`, `#3f3f46`)나 고채도 원색의 남발을 금지하고, 메인 IDE 테마 변수를 일관되게 사용합니다:
- **팝업 컨테이너 배경**: `--bg-surface` (`#1e202b`)
- **내부 목록 및 입력창 배경**: `--bg-main` (`#121318`) 또는 `--bg-editor` (`#16171e`)
- **테두리 경계선 (Border)**: `--border-color` (`#2e3142`)
- **호버 및 행 선택 배경**: `--bg-surface-hover` (`#282a38`)
- **활성 포인트 및 메인 액션**: `--accent-primary` (`#6366f1` / 인디고 퍼플)
- **그림자 규칙**: 메인 시스템 스타일과 마찬가지로 과도한 소프트 섀도우를 배제하고 날렵한 1px 보더 중심의 하이엔드 IDE 룩 유지.

---

### 3. 수직 공간 효율성 & 핵심 뷰포트 확보
- **수직 적층(Stacking) 최소화**: 헤더, 탭, 알림창, 필터바 등이 수직으로 겹겹이 쌓여 본문 콘텐츠(파일 목록, 에디터, 폼)가 압축되는 현상을 방지합니다.
- **핵심 뷰포트 60% 이상 보장**: 모달 높이 내에서 실제 사용자가 상호작용할 본문 영역이 최소 60% 이상 확보되도록 알림 박스와 배너는 1~2줄 컴팩트 형태로 디자인합니다.
- **중복 CTA 배제**: 동일한 액션을 실행하는 버튼(예: 헤더 우측 버튼과 본문 중앙 버튼)을 중복 노출하지 않습니다.

---

### 4. 컴포넌트 조형 언어 일치 (IDE 스타일)
- **탭 스타일**: 둥근 알약형(Pill) 탭 대신, 메인 에디터 상단 탭과 유사한 플랫한 하단 보더 탭(`border-b-2 border-indigo-500`) 또는 플랫 세그먼트 스타일을 적용합니다.
- **인디케이터**: 시스템 내부 권한 코드(예: `Scope: drive.file`)와 같은 기술 용어는 직관적인 사용자 친화적 뱃지로 정제합니다.
